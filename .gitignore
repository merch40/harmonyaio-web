.vscode/
.DS_Store
Thumbs.db
*.log
node_modules/
.env
.env.local
# Cloudflare local dev secrets - NEVER commit these
.dev.vars
.dev.vars.*

# SSL certs if ever added locally
*.pem
*.key
*.p12

# Build output if build step added later
dist/
.wrangler/

# Local edit-pass scratch files (kept for rollback reference, not deployed)
*.bak
_patch.py
_patch_b64.txt
_lucide.txt

# Source audio files (keep out of /public, kept locally for re-encoding)
audio-source/
